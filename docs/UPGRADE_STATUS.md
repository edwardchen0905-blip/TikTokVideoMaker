# 升级状态（尚未通过最终Windows验收）

基线：TikTokVideoMaker_GitHub_Root.zip，仓库 fd7d9fa50e02a84c051807845f275378ee218df4。
未更换产品定位或技术栈；运行时只复用 FFmpeg/FFprobe、SQLite、pywebview。pywebview固定6.0以匹配已核查的平台实现；Playwright/pywinauto仅供开发验收，不交给用户安装。

## 已实现，等待整体交互验收

- 第4、10章：独立音乐页，同一assets库的Music_ID、真实元数据、试听、搜索、音乐池、删除和历史；任务保存最终曲目与快照；AI/auto拒绝执行，不再取首曲冒充AI。
- 第7、8、12章：商品独立/商品合集/本地分组、多模板、数量、轮换/随机、参数预览、完整来源快照。
- 第9章：逐图时长、总时长互斥、自定义模板、任务覆盖、六种转场、安全运动；FFmpeg负责底层媒体处理。
- 第12章：外部输出目录贯穿配置、任务、记录、预览、打开、导出、重新生成；完成时间倒序。
- 第13章：每条输出完整解码检查，MP4/H.264/1080×1920/30fps/时长/音轨验证。
- 第11、14章：关联视频的Caption/标签/开头/CTA/封面文字与独立发布商品选择；每个目标保存独立待发布记录。
- 第15章：副本归属、任务使用保护、删除后历史、缺失提示、缓存及下载/输出保留策略。输出清理只读取真实发布记录；无外部连接不会产生上传/发布成功。
- 第16、17章：文件对话框集中处理、原生UI线程调度、内部日志错误与取消区分；等待当前渲染后正常关闭；构建前风险审查见 WINDOWS_REVIEW.md。

## 当前证据

本地Linux：17项unittest通过，真实渲染覆盖音乐身份、转场、逐图时长、商品隔离/合集、外部目录、重新生成、重启和删除保护。10页面路由检查通过。

本地Chromium缺失且下载超时，因此目前没有本地浏览器交互通过结论。仓库工作流先执行真实DOM业务检查，再执行Windows源码测试、便携ZIP构建、原EXE原页面与原生对话框验收。仅成功跑完后才上传便携产物。

GitHub运行34334833310：真实界面业务通过，实际生成4条视频，完成播放、发布文字、待发布记录、重新生成、音乐历史/删除、商品关联生产、自定义模板保存。截图导航等待已进一步修正为等待目标页面激活，避免截图落在上一个页面。Windows因旧脚本未取得官方固定运行组件地址停止在准备步骤，尚未构建EXE。
新增超宽/超高图片四角标记回归本地通过：静止、缩放、平移的开始/中间/结束实际帧均保留四角。

## 外部等待

TikTok真实环境、图片下载/SKU映射/挂载/上传/发布、AI文案/选曲、Pixabay搜索下载均未接通，不模拟成功。

## 开源调研决定

FFmpeg/FFprobe直接承担处理与检查；MoviePy、ffmpeg-python不重复引入；Editly仅参考声明式配置；Huey/RQ不建立第二队列。ClipForge不同栈且部分音乐/缩放不符合要求，未复制源码；TikTok Viral Factory核心工作流未实现，不能复用作成品引擎。
