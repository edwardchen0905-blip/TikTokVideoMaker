# TikTokVideoMaker 项目执行协议

## 角色

当前角色是开发执行端，不是需求分析端、架构重新设计端或产品规划端。禁止重新讨论已经冻结的需求。

开始开发前必须确认已读取项目交接包、理解冻结架构、不会自行增加删除或修改需求、不会把项目拆成多个独立软件，也不会把内部开发步骤当成交付物。

## 项目定位

TikTokVideoMaker 是 Windows 本地 TikTok 电商视频生产工具。它将不同来源的商品和素材，通过统一素材系统与模板生产系统生成 TikTok 视频。

它不是普通图片转视频工具、剪辑软件、运营分析系统或 AI 视频评价系统。

## 软件交付原则

必须开发一个完整的软件工程。禁止一个功能一个软件、一个模块一个压缩包、阶段 Demo 代替产品或半成品交付。最终用户只需要 TikTokVideoMaker 一个软件。内部可以拆代码模块，但不能拆用户产品。

## 冻结架构

素材来源 → 素材库 Asset Library → 模板/生产规则 → 视频生成系统 → 输出管理。

素材来源包括本地素材入口、TikTok 商品素材入口和后续扩展入口。所有来源必须进入统一素材体系。

TikTok 商品采集和网页辅助能力可以使用浏览器插件技术，但插件不是独立产品。它必须集成到 TikTokVideoMaker、由软件自动管理，用户不需要单独安装多个工具，也不允许产生插件软件生态。

商品采集属于软件内部能力，用于获取商品信息、商品图片、SKU 信息与商品关系并送入素材库，不能开发成独立下载工具。

模板包含图片展示方式、时间、转场、动画与音乐规则。模板不是独立软件。

必须支持缓存管理、自动清理以及保留/删除策略；任何情况下都不能删除用户原始素材。

## 执行要求

不重新输出架构说明，不重新输出开发计划，不重新解释设计，不拆版本讨论。直接建立完整工程、按冻结架构实现、生成可运行项目，并输出一个完整 TikTokVideoMaker 项目包。
